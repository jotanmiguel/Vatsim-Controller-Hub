"""Backward-compatibility wrapper for the settings module."""

from utils.settings import BASE_DIR, SETTINGS_PATH as CONFIG_PATH, build_entry, load_settings as load_config, normalize_settings as normalize_config, save_settings as save_config